import { StyleSheet } from 'react-native';
import { color_blue } from '../../assets/variables';

export default styles = StyleSheet.create({
    container : {
        padding : 10,
        borderRadius : 50,
        backgroundColor : 'white'
    }
})